<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>

        {% if app.environment == 'dev' %}
            <script type="module" src="http://localhost:3000/src/main.jsx" crossorigin="anonymous"></script>
        {% else %}
            <script type="module" src="{{ asset('build/main.js') }}"></script>
        {% endif %}

    </head>
    <body>
        <div id="root">
            {{ 'SORA'|trans }}
        </div>
    </body>
</html>